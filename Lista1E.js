const prompt = require("prompt-sync") ();
let distância = Number(prompt("digite a distância da viagem (em km):"));
let consumo = Number(prompt("Digite o consumo
médio do
carro (km por litro):"));
let preço = Number (prompt ("Digite o preço da
gasolina(R$ por litro):"));
let litros_necessarios - distancia / consumo; let custo_total - litros_necessarios * preco; console. log("Litros necessários:", litros necessarios,"L";
console. log("Custo total da viagem: R$",custo total);
